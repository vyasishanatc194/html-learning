$('.data-tilt01').tilt({
  glare: true,
  maxGlare: .5,
  reset: true,
  scale: 1.05,
  easing: "cubic-bezier(.03,.98,.52,.99)",
  perspective: 1000,
  maxTilt: 50
});

const myVar = $('.main-banner-section::before').tilt()
$('.main-banner-section::before').tilt({
  glare: true,
  maxGlare: .5,
  reset: true,
  scale: 1.05,
  easing: "cubic-bezier(.03,.98,.52,.99)",
  perspective: 1000,
  maxTilt: 50
});